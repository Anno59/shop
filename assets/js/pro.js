/*
$(function () {
    var reNum;
    $('img').each(function () {
        reNum = Math.floor(Math.random().toFixed(2).slice(0,-1)*16);
        $(this).attr('src','assets/images/pro'+reNum+'.jpg');
    });
});
*/
